machine:

  python:
    version: 2.7.10

  services:
    - docker

dependencies:
  override:
    - pip install -U pip ansible
    - ansible --version
    - printf '[defaults]\nroles_path=../' >ansible.cfg


test:
  override:
    - ansible-playbook tests/docker.yml
